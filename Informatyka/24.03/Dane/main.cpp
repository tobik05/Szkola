#include <iostream>
#include <fstream>
#include <cmath>

using namespace std;

int rysuj(x){
    int N=0;
    if(2*x<=N){
        strzalka(x, 2*x);
        rysuj(2*x);
    }
    if(2*)
}

int main() {
    
}

